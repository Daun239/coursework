import { CheckTicket } from "../Types/CheckTicket";
import { GenericService } from "./GenericService";

export class CheckTicketService extends GenericService<CheckTicket> {
    // Add any custom methods if necessary for CheckTicket
}
