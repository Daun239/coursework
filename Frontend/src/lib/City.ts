import { City } from "../Types/City";
import { GenericService } from "./GenericService";

export class CityService extends GenericService<City> {
    // Add any custom methods if necessary for City
}
