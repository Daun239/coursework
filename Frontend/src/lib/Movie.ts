import { Movie } from "../Types/Movie";
import { GenericService } from "./GenericService";

export class MovieService extends GenericService<Movie> {
  // Add custom methods here if needed (e.g., filter by genre, language, etc.)
}
